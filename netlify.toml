# Netlify 部署設定

[build]
  publish = "."

# 所有路徑都直接回傳對應靜態檔（靜態 HTML，無需 SPA redirect）
[[redirects]]
  from = "/*"
  to   = "/:splat"
  status = 200

# 安全標頭
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
